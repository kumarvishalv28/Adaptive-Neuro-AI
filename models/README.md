Trained checkpoints are written here by `src/train_pytorch_real.py`.

Expected files:
- `pytorch_bci_model.pth`    best-validation checkpoint
- `training_history.pkl`     loss and accuracy curves

Checkpoints are excluded from version control by `.gitignore`.
